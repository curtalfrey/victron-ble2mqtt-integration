dashboards/legacy_nodered_flow.json
swarm/ha-discovery.env
swarm/health.env
swarm/victron-secrets.env
swarm/.env
swarm/*.bak.*
swarm/input-snapshot_*.txt

# tools reverse proxy auth file (generated locally, do not commit)
nginx/.htpasswd

# local env / secrets
.env
victron-secrets.env
ha-discovery.env
health.env
.backups/
backups/
_diag/
*.bak
*.env
adguard-exporter.password
*.secrets

# common temporary/ignored files
.backups/
*.bak*

# Victron hub wheels (populate via scripts/sync-victron-wheels-from-hub.sh)
wheels/*
!wheels/.gitkeep

# Python artifacts
__pycache__/
*.py[cod]
.venv/
venv/

# swarm / stack secrets and overrides (do not commit real secrets)
swarm/victron-secrets.env
swarm/ha-discovery.env
swarm/health.env

# SSL materials for tools reverse proxy (generated locally)
ssl/*.key
ssl/*.crt
