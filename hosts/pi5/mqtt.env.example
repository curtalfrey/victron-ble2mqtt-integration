# Copy to mqtt.env (gitignored) or let scripts/deploy_pi5.sh write it from .env.
# MQTT_HOST must be the Pi 4 broker, never 127.0.0.1 from this host.
MQTT_HOST=192.168.0.223
MQTT_PORT=1883
MQTT_USERNAME=victron
MQTT_PASSWORD=
